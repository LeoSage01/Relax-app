


export const metadata = {
    title: 'Relax App Admin',
    description: 'Relax Admin Dashboard',
}


const layout = ({children} : {  children: React.ReactNode}) => {
  return (
    <div>
    {children}
    </div>
  )
}

export default layout